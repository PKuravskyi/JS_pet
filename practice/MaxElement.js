// Implement a function to find the maximum element in an array

const maxElementOf = arr => Math.max(...arr);

const array = [1, 5, -10, -1, 0];
console.log(`Maximum element of ${array} is ${maxElementOf(array)}`);
